name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies (Dugout Dynasty)
        run: npm ci

      - name: Run tests
        run: npm run test:run

      - name: Build Dugout Dynasty
        run: npx vite build

      - name: Install dependencies (Fantasy Tool)
        working-directory: fantasy-draft-tool
        run: npm ci

      - name: Fetch latest data (Fantasy Tool)
        working-directory: fantasy-draft-tool
        run: npm run fetch-data

      - name: Build Fantasy Tool
        working-directory: fantasy-draft-tool
        run: npm run build

      - name: Prepare Pages artifact
        run: |
          mkdir -p _pages
          cp -r dist/* _pages/
          mkdir -p _pages/fantasy-draft-tool
          cp -r fantasy-draft-tool/dist/* _pages/fantasy-draft-tool/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _pages

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
