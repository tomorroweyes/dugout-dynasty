import MatchLog from "@/components/MatchLog";

/**
 * MatchPanel - Game Log View
 * Displays match history with expandable details (boxscore and play-by-play)
 * Playing matches is now done from the League page
 */
function MatchPanel() {
  return <MatchLog />;
}

export default MatchPanel;
